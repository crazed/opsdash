.well(style="padding: 8px 0")
  %ul.nav.nav-list
    %li.nav-header= "#{plugin_name} Navigation"
    - sidebar_links.each do |link|
      - if link[:name]
        %li(class="#{sidebar_link_active?(link[:link]) ? 'active' : ''}")
          %a(href="#{link[:link]}")= link[:name]
